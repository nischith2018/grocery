import { Component } from '@angular/core';

@Component({
  template : `
    <login-layout>
      <ng-container navbar>
        <navbar></navbar>
      </ng-container>
      <login-app></login-app>
    </login-layout>
  `
})

export class LoginPage{

}